# MClash 1.3.4

This patch fixes Profile activation for valid Mihomo subscription YAML that
uses indentless sequences under top-level mapping keys.

## Profile activation

- Accepts standard YAML such as `proxies:` followed by unindented `-` items
  during the port-conflict preflight.
- Keeps rejecting malformed root-level sequence documents and unsupported
  listener binding syntax.
