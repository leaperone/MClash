# MClash 1.5.11

This fix-forward patch keeps the compatibility integration smoke test on its
explicit legacy connector after the native runtime became the default. Native
launches and native-only packages are unchanged; the rollback boundary is now
also enforced by the release validation harness.
