# MClash 1.6.2

这次更新把节点切换改成了更直接的组内成员列表。

- 选中任意节点组后，右侧直接显示该组当前可用的成员。
- 点击成员即可切换当前出口。节点名称、当前选择、固定偏好、可用状态和延迟显示在同一列表中。
- URL Test 和 Fallback 组支持固定一个偏好节点，也支持恢复自动选择。
- 日本、美国、香港和普通节点组使用同一套操作方式，不再有单独的区域按钮。
- 组编辑器改成次要操作，日常切换不再进入配置编辑页面。

从 1.6.1 升级不需要重新导入节点或重建规则。现有来源、节点组、规则和连接设置会继续保留。
