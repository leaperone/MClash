# MClash 1.4.19

This patch republishes 1.4.18 after correcting release verification.

- Keep Parsec (`tv.parsec.www`) on the native App Routing direct path so
  peer-to-peer and LAN traffic is not sent through Mihomo.
- Verify that rule in the capture plan, not in generated Mihomo YAML.
- Keep private and link-local network protection ahead of the catch-all rule.

MClash still does not enable TUN by default.
